# Automation Orchestrator

Config-driven automation product built on JohnEngine.

## Setup
1. Install dependencies:
   - `pip install -r requirements.txt`
2. Set config (optional):
   - `set AO_CONFIG=./config/sample_config.json`

## Run
- `python -m automation_orchestrator.main`

## Notes
- Replace the sample config with real workflows.
