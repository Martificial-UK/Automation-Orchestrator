# Docker single-host environment template
# Copy to a secure location on the host, e.g. /opt/automation-orchestrator/secrets/.env
# Do NOT commit real values to git.

# Core secrets
JWT_SECRET=replace-with-strong-secret
LICENSE_SECRET=replace-with-strong-secret

# Database (optional; used by docker-compose db service)
DB_USER=orchestrator
DB_PASSWORD=replace-with-strong-password
DB_NAME=automation_orchestrator

# App settings
LOG_LEVEL=INFO
DATABASE_URL=sqlite:///./automation_orchestrator.db

# Optional: Redis (if you run an external Redis)
# REDIS_URL=redis://redis:6379/0
